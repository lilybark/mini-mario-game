//
// Created by meili on 6/8/2024.
//

#include "BoxedIn.h"
#include "entities/Rectangle.h"

namespace MiniMario {
    void BoxedIn::start() {
        /*
        auto *left = new Rectangle();
        auto *right = new Rectangle();
        auto *up = new Rectangle();
        auto *down = new Rectangle();
        auto *front = new Rectangle();
        auto *back = new Rectangle();

        left->moveTo({-1.0, 0.0, 0.0});

        this->entities.push_back(left);
        this->entities.push_back(right);
        this->entities.push_back(up);
        this->entities.push_back(down);
        this->entities.push_back(front);
        this->entities.push_back(back);
        */
    }

    void BoxedIn::update(double d) {

    }

    void BoxedIn::stop() {

    }
} // MiniMario